# TODO SEEDMS rename this file to the name provided in systemd-install.sh
# TODO SEEDMS modify config values here to match default micro-service config
service:
  registerInterval: 5s
database:
  userName: root
  password:
  host: localhost:26257
  dbName: seedms
  sslCert: /etc/cockroachdb/certs/node.cert
  sslKey: /etc/cockroachdb/certs/node.key
  sslRootCert: /etc/cockroachdb/certs/ca.cert
token:
  tokenKeyFile: ../../../github.com/tomogoma/authms/ssh-keys/sha256.key
